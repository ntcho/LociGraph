import type { RelationQuery } from "~types";

// Use regex `\[(.+?), (.+),` and list with 
// `{ entity: "$1", attribute: "$2" },` to generate this list

const query: RelationQuery[] = [
]

export default query;